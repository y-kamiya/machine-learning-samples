use rand::prelude::*;
use maze::{Action, Field, Pos, NodeType};

use mcts::create_field;

fn mcts() {
    let field = create_field(true);

    // let tree = 
    for i in 0..10 {
    }


}

fn main() {
    // random_maze();
    mcts();
}
